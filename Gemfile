# frozen_string_literal: true

source 'https://rubygems.org'

# RSpec is a testing framework for Ruby.
gem 'rspec'

# HTTParty is an HTTP client library, making it easy to send HTTP requests.
gem 'httparty'

# JSON-Schema is used for validating JSON data against a schema.
gem 'json-schema'

gem 'rubocop', '~> 1.63'
gem 'selenium-webdriver'

gem 'debase', '3.0.0.beta.11'
gem 'ruby-debug-ide', '3.0.0.beta.17'
